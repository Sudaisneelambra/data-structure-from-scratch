const arr=[1,2,3,'sudais']

// adding the element in the array to end
arr.push(4)

// adding the element in the array to start
arr.unshift(0)

// removing the element in the array from end
arr.pop()

// removing the element in the array from start
arr.shift()


  for (const item of arr){
    console.log(item);
  }

//   map,filter,reduce,concat,slice and splice